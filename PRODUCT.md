# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

面向希望从参考照片提取色彩风格，并将其批量应用到自己照片的摄影师、影像创作者和调色爱好者。用户既可能在桌面端处理 RAW 与高分辨率文件，也可能在手机端快速预览和管理风格。

## Product Purpose

调色室 / Color Lab 在浏览器本地分析参考照片的影调、色彩、光线、语义区域和质感，并把风格迁移到一张或多张目标照片。成功意味着用户能够获得可信、可调、可保存和可导出的仿色结果，并可自行决定是否把原图、历史与完整风格私有同步到云端。

## Positioning

产品以“参考图片驱动的本地语义仿色”为核心机制：不是套用固定滤镜，而是根据用户提供的参考照片生成可编辑的风格档案，并保留专业曲线、基本调整、RAW 与预设导出能力。

## Operating Context

用户从品牌一级页面注册或登录后进入专业仿色工作台；上传参考照片和待调色照片，等待本地分析，调整强度、基本参数、曲线和颗粒，最后导出照片或调色预设。账户、完整风格与用户选择同步的照片可跨设备恢复。桌面 Chrome/Edge 是完整专业体验，其他浏览器和手机提供兼容路径。

## Capabilities and Constraints

- 照片像素与仿色分析始终在浏览器本地完成；云端同步只保存用户开启同步后导入的原始文件，不在服务器运行仿色分析。
- 生产账户使用 Cloudflare D1、PBKDF2 密码派生与 HttpOnly 会话，不保存明文密码；“记住我”保持 30 天登录。
- 私有照片对象保存到 R2，下载、恢复与删除都校验当前账号；完整 V4 风格在本机 IndexedDB 与云端之间同步。
- V4 使用可逆的光线归一化与末端高光色彩保护，避免暗场照片的亮区先截平、再压成灰色平台；人物局部迁移只在可靠人像模型证据存在时启用。
- 导入过程逐文件显示文件名、字节读取量、当前张数和分析阶段；手机端使用压缩工作预览、串行分析、模型超时与 RAW 降级，避免无限停在读取页。
- 当前账户仅使用用户名与密码，尚未提供找回密码流程。
- 保留现有 Color Engine 4、RAW、批量目标照片、实时基本调整与曲线、滤镜保存及多格式导出。
- Color Engine 5.1 为每张目标照片提供可叠加的画笔、线性渐变、径向渐变、主体与天空蒙版，支持添加、减去、反相及局部影调/颜色调整；裁切、旋转、水平与垂直透视、缩放和偏移同时作用于实时预览与原尺寸批量导出。

## Brand Commitments

- 产品名称保留“调色室 / Color Lab”，不因参考提示词更名为 Foldcraft。
- 一级页面采用用户指定的全屏视频英雄页结构。
- 一级页与专业工作台统一采用 Apple OS 26 液态玻璃风格，同时保证清晰层级、文字可读性、键盘焦点和移动端触控。
- 品牌语气专业、克制、具有影像创作感，不夸大对专有相机算法的复刻程度。

## Evidence on Hand

- 已有可运行并上线的 Color Engine 4 工作台与自动质量门禁。
- 用户提供了背景视频 URL、英雄页结构、导航、文案层级和动效要求。
- 暂无可公开使用的客户证言、商业数据或价格，页面不得虚构。

## Product Principles

1. 私密优先：默认本地分析、可关闭的云端同步和明确的数据边界。
2. 结果可信：保护肤色、高光、影调连续性，并允许用户实时修正。
3. 专业但可进入：一级页面降低理解门槛，工作台保持完整专业能力。
4. 状态透明：分析、渲染、导出和错误都提供即时反馈。
5. 渐进增强：高性能设备获得完整体验，受限设备使用兼容路径。

## Accessibility & Inclusion

界面需支持键盘导航、可见焦点、表单显式标签、错误就近提示、足够的文字对比度、44px 触控目标，并尊重 `prefers-reduced-motion`。
